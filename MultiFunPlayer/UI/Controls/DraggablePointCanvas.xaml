<Canvas x:Class="MultiFunPlayer.UI.Controls.DraggablePointCanvas"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:MultiFunPlayer.UI.Controls" 
        xmlns:material="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d" 
        d:DesignHeight="450" d:DesignWidth="800"
        Background="Transparent"
        MouseDown="OnMouseDown"
        MouseUp="OnMouseUp"
        MouseMove="OnMouseMove">
    <Popup Name="Popup" 
           IsOpen="False" 
           Placement="Relative" 
           AllowsTransparency="True"
           PlacementTarget="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Canvas}}}">
        <Grid>
        <material:Card Margin="5">
            <TextBlock Text="{Binding PopupText, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Canvas}}}"
                       Margin="4 2 4 2"/>
            </material:Card>
        </Grid>
    </Popup>
</Canvas>


